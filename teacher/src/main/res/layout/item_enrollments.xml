<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="@color/maverkick_light_gray">

    <!-- Enrollment Label -->
    <TextView
        android:id="@+id/tv_enrollmentLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/enrollments_label"
        android:textSize="14sp"
        android:textColor="@color/maverkick_main"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Icon for Enrollments -->
    <ImageView
        android:id="@+id/ic_enrollment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_enrollments"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_enrollmentLabel"
        app:tint="@color/maverkick_light_green" />

    <!-- Enrollment count -->
    <TextView
        android:id="@+id/tv_enrollmentCount"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:textColor="@color/maverkick_black"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/ic_enrollment"
        app:layout_constraintTop_toTopOf="@id/ic_enrollment"
        app:layout_constraintBottom_toBottomOf="@id/ic_enrollment" />


</androidx.constraintlayout.widget.ConstraintLayout>
