<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/teacher_bottomNavigationView"
        style="@style/CustomBottomNavigationView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/teacher_bottom_navigation_menu"
        app:itemIconTint="@color/bottom_navigation_colors"
        app:labelVisibilityMode="unlabeled"/>

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/teacher_navHostFragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/teacher_bottomNavigationView"
        app:defaultNavHost="true"
        app:navGraph="@navigation/teacher_nav_graph" />

</androidx.constraintlayout.widget.ConstraintLayout>